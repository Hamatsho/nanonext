import Head from 'next/head';
//import parse from 'html-react-parser';
//const parse = require('html-react-parser').default;

const Seo = ({ seoHtml }) => {
  //if (!seoHtml) return null;
//const meta = parse(seoHtml)
//console.log("&&&&&&", meta)
  return (
    <Head>
      <title>Nahool</title>
    
    </Head>
  );
};

export default Seo;
